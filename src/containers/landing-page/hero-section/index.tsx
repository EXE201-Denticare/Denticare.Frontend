import React from "react"

import { SwipeCarousel } from "@/containers/landing-page/hero-section/swipe-carousel"

function HeroSection() {
  return (
    <section>
      <SwipeCarousel />
    </section>
  )
}

export default HeroSection
